streamlit==1.35.0
requests==2.32.3
Pillow==10.3.0             # For image preview/processing
pdf2image==1.17.0          # Convert PDFs to images (for optional preview)
pytesseract==0.3.13        # Run OCR from frontend (optional)